{
	"type": "module",
	"license": "MIT",
	"scripts": {
		"test": "rimraf bin && haxe test.hxml && node --no-warnings --experimental-modules --es-module-specifier-resolution=node bin/test.js",
		"postinstall": "lix download"
	},
	"devDependencies": {
		"@types/node": "^12.7.2",
		"lix": "^15.4.2",
		"rimraf": "^2.7.0"
	}
}
