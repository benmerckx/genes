{
	"type": "module",
	"license": "MIT",
	"scripts": {
		"test": "haxe test.hxml && tsc && node --no-warnings --experimental-modules --es-module-specifier-resolution=node bin/test.js",
		"postinstall": "lix download"
	},
	"devDependencies": {
		"lix": "^15.6.0-alpha.4",
		"typescript": "^3.7.3"
	}
}
